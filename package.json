{
  "name": "ids-enterprise-wc-docs",
  "version": "0.0.15",
  "description": "Documentation website that covers the IDS Web Components source code",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf \"build\"",
    "build": "jsdoc -c jsdoc.json",
    "serve": "live-server \"build/\"",
    "start": "npm run build && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ed Coyle (ed.coyle@infor.com)",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "ids-enterprise-wc": "0.0.0-beta.15"
  },
  "devDependencies": {
    "eslint": "^8.2.0",
    "jsdoc": "^3.6.7",
    "live-server": "^1.2.1",
    "rimraf": "^3.0.2"
  }
}
